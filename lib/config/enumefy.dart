enum TransactionType { success, failure }

enum ThemeModeType { lightMode, darkMode, systemMode }

enum LogType { error, warning, info, trace }

enum DashboardSkeletonCategoryType { appBar, ads }

enum TransactionsSkeletonCategoryType {
  lastTransactions,
  todayTransactions,
  accounts
}

enum TransfersSkeletonCategoryType { beneficiaries, financialInstitutions }

enum ProfileSkeletonCategoryType { faqs, privacyPolicy }

enum AccountType { internal, external }

enum PaymentsSkeletonCategoryType {
  airtimeProviders,
  electricityProviders,
  cablePackages,
  cableProviders
}
